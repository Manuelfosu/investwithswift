{
  "name": "swift-investments-backend",
  "version": "1.1.0",
  "description": "Swift Investments backend (Node http + PostgreSQL/Neon + crypto). Falls back to local SQLite when DATABASE_URL is unset.",
  "private": true,
  "type": "commonjs",
  "engines": { "node": ">=22" },
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js"
  },
  "dependencies": {
    "pg": "^8.13.1"
  }
}
